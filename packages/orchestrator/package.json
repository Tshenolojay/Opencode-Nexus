{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/orchestrator",
  "version": "1.17.14",
  "description": "Intelligent orchestration layer for OpenCode — task classification, confidence estimation, agent dispatch, and prompt augmentation",
  "private": true,
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@opencode-ai/schema": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
